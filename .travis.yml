sudo: required

os: linux

dist: trusty

services: docker

language: cpp

compiler: gcc

before_install:
  - docker pull ubuntu:latest

install:
#  - docker build github.com/schneeheld/oTravis

before_script:
#  - make test
#  - ./oTravis
#  - ls -l
#  - uname -a
#  - cat Makefile

script: 
#  - docker version
  - docker images
  - docker run -d --name=ub1604 ubuntu
#  - docker run ubuntu
  - docker ps -a
#  - docker exec ubuntu apt-get install g++
  - docker exec ub1604 g++ --version
  - docker exec ub1604 ls -l
  - docker exec ub1604 make test
  - docker exec ub1604 ./oTravis
#  - docker exec ub1604 pwd
#  - docker exec ubuntu cd /home
#  - docker exec ububtu ./oTravis
  - docker stop ubuntu
  - docker ps -a
#  - docker run -it ubuntu
#  - apt-get update
#  - uname -a
#  - make test
  

notifications:
  email: false
