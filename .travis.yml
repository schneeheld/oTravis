sudo: required

os:
  - linux

dist: trusty

services:
  - docker

language: cpp

compiler:
  - gcc

before_install:
  - docker pull ubuntu

before_script:
  - make test
  - ./oTravis
  - ls -l
  - uname -a
  - cat Makefile

script: 
  - docker version
  - docker images
  - docker run -t -d --name=ub1604 ubuntu
#  - docker run ubuntu
  - docker ps -a
#  - docker exec ub1604 make test
#  - docker exec ub1604 ./oTravis
  - docker exec ub1604 ls -l
  - docker exec pwd
  - docker stop ub1604
  - docker ps -a
#  - docker run -it ubuntu
#  - apt-get update
#  - uname -a
#  - make test
  

notifications:
  email: false
